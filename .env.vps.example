# NEXUS Idea Analyzer - VPS Environment Variables
# Copy this file to .env and fill in your actual values

# =============================================================================
# DOMAIN CONFIGURATION
# =============================================================================
# Your NEXUS domain (e.g., nexus.yourdomain.com)
NEXUS_DOMAIN=nexus.yourdomain.com

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL credentials
DB_USER=nexus
DB_PASSWORD=CHANGE_ME_TO_SECURE_RANDOM_PASSWORD
DB_NAME=idea_analyzer

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================
# Redis password for cache/queue
REDIS_PASSWORD=CHANGE_ME_TO_SECURE_RANDOM_PASSWORD

# =============================================================================
# API AUTHENTICATION
# =============================================================================
# Secret API key for protecting extractor endpoints
# Generate with: openssl rand -hex 32
NEXUS_API_KEY=CHANGE_ME_TO_SECURE_RANDOM_API_KEY

# =============================================================================
# WHISPER CONFIGURATION
# =============================================================================
# Whisper model for audio transcription
# Options: tiny, base, small, medium, large
# Recommendation: base (good balance of speed/quality)
WHISPER_MODEL=base

# =============================================================================
# SLACK BOT CONFIGURATION
# =============================================================================
# Get these from https://api.slack.com/apps
# Required for Slack integration

# Bot User OAuth Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Signing Secret (for verifying requests)
SLACK_SIGNING_SECRET=your-signing-secret-here

# App-Level Token for Socket Mode (starts with xapp-)
SLACK_APP_TOKEN=xapp-your-app-token-here

# Required Slack Bot Token Scopes:
# - chat:write
# - channels:history
# - files:read
# - app_mentions:read
#
# Socket Mode must be enabled with:
# - connections:write

# =============================================================================
# OPTIONAL: REDDIT API
# =============================================================================
# Get from https://www.reddit.com/prefs/apps
# Leave empty if not using Reddit extraction

REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=IdeaAnalyzer/1.0

# =============================================================================
# OPTIONAL: GITHUB API
# =============================================================================
# Personal Access Token from https://github.com/settings/tokens
# Leave empty if only extracting public repositories

GITHUB_TOKEN=

# =============================================================================
# NOTES
# =============================================================================
#
# Password Generation:
# Generate secure random passwords with:
#   openssl rand -base64 32
#   or
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# API Key Generation:
#   openssl rand -hex 32
#
# Whisper Models (RAM usage and quality):
#   tiny   - ~1GB RAM, fast, basic accuracy
#   base   - ~1GB RAM, fast, good accuracy (RECOMMENDED)
#   small  - ~2GB RAM, medium speed, better accuracy
#   medium - ~5GB RAM, slower, even better accuracy
#   large  - ~10GB RAM, slowest, best accuracy
#
# Security:
# - Never commit this file to Git (already in .gitignore)
# - Store production secrets in password manager
# - Use different passwords for each service
# - Rotate API keys periodically
#
# Coolify Deployment:
# In Coolify, set these as environment variables for each service.
# Do not include them in docker-compose.vps.yml directly.
